#include "./include/rwMemory.h"
#include <windows.h>
#include <iostream>
#include <string>

using namespace std;

int main(){
    string exeName;
    string memAddr;
    int offset;
    float memVal;
    cout << "ingresa el nombre del ejecutable: " << endl;
    cin >> exeName;
    cout << "ingresa la direccion de memoria: " << endl;
    cin >> memAddr;
    cout << "ingresa el offset de la direccion: " << endl;

    // arreglar vector de DWORD antes de seguir con esto

    return 0;
}